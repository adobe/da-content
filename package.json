{
  "name": "da-content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy:prod": "wrangler deploy",
    "deploy:stage": "wrangler deploy --env stage",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.5.0",
    "wrangler": "4.33.1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.878.0",
    "cookie": "1.0.2"
  }
}
