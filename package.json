{
  "name": "da-content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy:prod": "wrangler deploy",
    "deploy:stage": "wrangler deploy --env stage",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "vitest": "3.2.4",
    "wrangler": "4.33.1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.878.0",
    "cookie": "1.0.2"
  }
}
